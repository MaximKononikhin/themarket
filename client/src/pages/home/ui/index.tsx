import React from "react";

import { Layout, Typography } from "@shared/components";

export const HomePage = () => (
    <Layout>
        <Typography type="header-1">HOME</Typography>
    </Layout>
);
